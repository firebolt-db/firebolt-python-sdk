name: Pull request

on: push

jobs:
  check-title:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v2
      
      - name: run
        run: |
          chmod +x dev_cli_linux_amd64
          ( echo token; echo key; echo secret; echo "${{ secrets.TEMP_USERNAME }}"; echo "${{ secrets.TEMP_PASSWORD }}"; ) | ./dev_cli_linux_amd64 authenticate --debug
