name: Pull request

on: push

jobs:
  check-title:
    runs-on: ubuntu-latest
    steps:
      - name: run
        run: |
          chmod +x dev_cli_linux_amd64
          { echo token; echo key; echo secret; echo email; echo password; } | ./dev_cli_linux_amd64 authenticate
